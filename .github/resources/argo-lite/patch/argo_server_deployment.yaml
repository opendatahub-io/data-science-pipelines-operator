apiVersion: apps/v1
kind: Deployment
metadata:
  name: argo-server
  namespace: argo
spec:
  template:
    spec:
      containers:
        - name: argo-server
          resources:
            limits:
              cpu: 20m
              memory: 500Mi
            requests:
              cpu: 20m
              memory: 100Mi
